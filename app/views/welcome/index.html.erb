<h1>This better look like bootstrap!</h1>

<div>
	<% if cookies[:auth_token] %>
		<%= link_to "Log Out", auth_path, method: :delete %>
	<% else %>
		<%= link_to "Sign Up", new_user_path %> | <%= link_to "Login", new_auth_path %>
	<% end %>
</div>